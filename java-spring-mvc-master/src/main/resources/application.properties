spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3307/laptopshop
spring.datasource.username=spring
spring.datasource.password=spring
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


#config view engine
# spring.mvc.view.prefix=/WEB-INF/view/
# spring.mvc.view.suffix=.jsp

#default = 1MB
spring.servlet.multipart.max-file-size=50MB

#default = 10 MB (form data)
spring.servlet.multipart.max-request-size=50MB


logging.level.org.springframework.security=DEBUG

#config session
spring.session.store-type=jdbc
spring.session.timeout=30m
spring.session.jdbc.initialize-schema=always
#server.servlet.session.timeout
#spring.session.jdbc.table-name=SPRING_SESSION


# Số lượng kết nối tối đa trong pool
spring.datasource.hikari.maximum-pool-size=10

# Số lượng kết nối giữ ở trạng thái rảnh (idle)
spring.datasource.hikari.minimum-idle=5

# Thời gian tối đa kết nối có thể rảnh trước khi bị thu hồi (ms)
spring.datasource.hikari.idle-timeout=30000

# Thời gian sống tối đa của một kết nối (ms)
spring.datasource.hikari.max-lifetime=1800000

# Thời gian chờ kết nối trước khi bị lỗi (ms)
spring.datasource.hikari.connection-timeout=20000


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=phuochuu0512@gmail.com
spring.mail.password=dyhu nlsx tudd wuau
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
